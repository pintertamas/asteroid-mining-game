package Playground;

/**
 * Enum osztály.
 * A játék állapotai.
 */
@SuppressWarnings("SpellCheckingInspection")
public enum GameState {
    LOAD,
    IN_PROGRESS,
    WON,
    LOST,
    RESET,
    IDLE,
}
